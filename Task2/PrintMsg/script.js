var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
 msg += "hi";
 }
if (lemeout) {
 msg += 'Hello';
}
console.log(msg);

//only if lemein loop will get executed bez we are assign some string value to the lemein variable
//Whereas lemeout is assigned with 0 value hence the lemeout if loop will not get executed 